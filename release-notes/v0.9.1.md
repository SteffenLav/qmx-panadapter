## Highlights  Single-fix point release: screenshot capture now holds the display mutex for the full snapshot, so the resulting PNG is never a partially-rendered frame.  ## What's new  ### Screenshot mutex fix The Phase 5.11 screenshot tool used a non-blocking display lock and could capture a partially-rendered frame - visible as a wrapped bottom status bar in earlier hero images. Switched to `bsp_display_lock(portMAX_DELAY)`. The snapshot now waits for in-flight LVGL operations to finish.  This fix is also bundled into v0.9.2 onwards.  ## Flashing  No merged binary attached to this release. The fix landed in v0.9.2 with a merged binary attached - upgrade to v0.9.2+ for a one-step web-flasher experience.  ---  **Full changelog:** [v0.9.0...v0.9.1](https://github.com/SteffenLav/qmx-panadapter/compare/v0.9.0...v0.9.1)  73 de OZ1LAV