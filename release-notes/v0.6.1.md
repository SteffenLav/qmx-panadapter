## Highlights  Plug-and-play release  no more manual QMX menu setup before using the panadapter.  ## What's new  ### Auto-enable QMX IQ mode (Phase 5.10J) The Tab5 now sends `Q9 1;` to the QMX as soon as the CAT link is established, automatically putting the radio into IQ output mode for the session. No need to enable IQ Mode in the QMX menu first.  The Q9 command is a session-only toggle on the QMX (not written to EEPROM), so the user'\''s normal radio configuration is automatically restored on Tab5 disconnect or QMX power-cycle. Plug in the Tab5 for SDR / panadapter use, unplug to go back to normal SSB / digi / CW operation.  ### Documentation Added the first real-hardware photo to the README (FT8 on 20 m).  ## Hardware required  - M5Stack Tab5 (ESP32-P4, ST7123 panel variant) - QRP Labs QMX or QMX+ transceiver - USB-C OTG cable between them  ## Flashing  Merged binary `qmx_panadapter_merged.bin` attached below. Drop into [web.esphome.io](https://web.esphome.io/) from Chrome or Edge.  ---  **Full changelog:** [v0.6.0...v0.6.1](https://github.com/SteffenLav/qmx-panadapter/compare/v0.6.0...v0.6.1)  73 de OZ1LAV